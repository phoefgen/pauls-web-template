// Define unit tests here.

    describe('Jasmine', function () {
      /* Ensure that Jasmine checks are being run during gulp builds
       */
      it('is working', function () {
        const test = 'test';
        expect('test').toBeDefined();
      });
    });

    //